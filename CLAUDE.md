- don't glaze me, i appreciate an encouraging tone, but constructively challenge my thinking (though not just for the sake of challenging -- so we get to the better choices)

- **NEVER**, ever, hardcode variables. Always use a config

- When something doesn't work as expected don't just workaround, debug it, fix it, talk to me about it; for example, when testing, if a test isn' passing and it's because you think it's just a minor issue, don't pass the test! we probably need to fix it

- similarly if a module isn't present, don't just workaround, find it, install it, talk to me about it

- **ALWAYS** use type hinting and robust documentation when writing python code (docstrings, etc)

- **ALWAYS** use context7 to reference non-standard python package and library docs when coding

